<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.elenx.fishmash" >

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity android:name="net.elenx.fishmash.activities.AuthenticateActivity"/>
        <activity android:name="net.elenx.fishmash.activities.LearningActivity"/>

        <activity
            android:name=".activities.MainActivity"
            android:theme="@style/splashScreenTheme"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="net.elenx.fishmash.activities.MenuActivity"/>
        <activity android:name="net.elenx.fishmash.activities.PickWordListActivity"/>
        <activity android:name="net.elenx.fishmash.activities.ProfileActivity"/>
        
    </application>
</manifest>
